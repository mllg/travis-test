language: c
sudo: false

env:
  - R_LIBS_USER=~/R/library PATH=$HOME/texlive/bin/x86_64-linux:$HOME/pandoc/:$HOME/r-builder-travis-3.2.3/R-3.2.3/bin:$PATH

cache:
  directories:
    $R_LIBS_USER

before_install:
  - mkdir -p "$R_LIBS_USER"
  - export PATH=$HOME/bin:$HOME/texlive/bin/x86_64-linux:$HOME/r-builder-travis-3.2.3/R-3.2.3/bin/:$PATH

install:
  - chmod u+x setup.sh && ./setup.sh
  - Rscript -e 'if (length(find.package("devtools", quiet = TRUE)) == 0L) { install.packages("devtools", repos = "http://cran.rstudio.com") }'
  - Rscript -e 'devtools::update_packages("devtools", repos = "http://cran.rstudio.com")'
  - Rscript -e 'devtools::install_deps(repos = "http://cran.rstudio.com", dependencies = TRUE)'

script:
  - Rscript -e 'devtools::check()'
