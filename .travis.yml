sudo: required

services:
  - docker

before_install:
  - docker pull rocker/r-devel
  - docker run -d --name devel -v $(pwd -P):/mnt rocker/r-devel

script:
  # install package dependencies
  - docker exec devel /bin/sh -c "cd /mnt;Rscript -e 'devtools::install_deps(dep=T);devtools::check()'"
